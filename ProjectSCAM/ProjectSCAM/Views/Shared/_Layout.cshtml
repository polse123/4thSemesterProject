<!DOCTYPE html>
<html>
<head>

    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - SCAMS</title>
    @Styles.Render("~/Content/css")
    @Styles.Render("~/Content/alarmPopup.css")
    @Scripts.Render("~/Scripts/shared.js")
    @Scripts.Render("~/bundles/modernizr")
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.10.1/jquery.min.js"></script>
    <script>
        function CheckPopup() {
            $.get("/alarm/Popup", function (data, status) {
                console.log("out");
                var dat = JSON.parse(data);
                if (dat === true) {
                    $.get("/alarm/GetAlarm", function (data, status) {
                        var json = JSON.parse(data);
                        document.getElementById("modalAlarmId").textContent = json.Id;
                        document.getElementById("modalBatchId").textContent = json.BatchId;
                        document.getElementById("modalTimeStamp").textContent = json.Timestamp;
                        document.getElementById("modalMachineId").textContent = json.MachineId;
                        document.getElementById("modalStopReason").textContent = json.StopReason;
                    });
                    var element = document.querySelector(".modal");
                    element.style.display = "inline";
                } else {
                    var element = document.querySelector(".modal");
                    element.style.display = "none";
                };
            }
            )
        };
        function handleAlarm() {
            $.post("/alarm/handle", { command: "oof" });
        };
        function shouldEnableAlarms() {
            var type = getUserType();
            console.log(type);
            if (getUserType() == 1) {
                console.log("2");
                setInterval(CheckPopup, 1000);
            } else {
                console.log("ahh");
            }
        }
        var retrieveInterval;
        clearInterval(retrieveInterval);
        getUserType();
    </script>

</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink("SCAMS", "Index", "Login", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li class="userLinks">@Html.ActionLink("Overview", "Index", "Overview")</li>
                    <li class="userLinks">@Html.ActionLink("Machine Selection", "Index", "MachineSelection")</li>
                    <li class="userLinks">@Html.ActionLink("Production", "Index", "Production")</li>
                    <li class="userLinks">@Html.ActionLink("Batches", "Index", "Batch")</li>
                    <li class="userLinks">@Html.ActionLink("Alarms", "Index", "Alarm")</li>
                  
                    <li class="userLinks">@Html.ActionLink("Optimization", "Index", "Optimization")</li>
                    <li class="userLinks">@Html.ActionLink("Tracking", "Index", "Tracking")</li>
                    <li class="adminLinks">@Html.ActionLink("User Creation", "Index", "UserCreation")</li>
                    <li class="adminLinks">@Html.ActionLink("User Management", "Index", "UserManagement")</li>
                </ul>
                @Html.Partial("_LoginPartial")
            </div>
        </div>
    </div>
    <!-- The Modal -->
    <div id="alarmModal" style="display:none;" class="modal">

        <!-- Modal content -->
        <div class="modal-content">
            <div class="modal-header">
                <span class="close">&times;</span>
                <h2>Alarm!</h2>
            </div>
            <div class="modal-body">
                <p>Alarm info:</p>
                <table class="popupstableclass" id="popuptableid">
                    <tr>
                        <th>AlarmID:</th>
                        <th>BatchID:</th>
                        <th>Date/time:</th>
                        <th>Stop reason:</th>
                        <th>MachineID:</th>
                    </tr>
                    <tr>
                        <td><span id="modalAlarmId"></span></td>
                        <td><span id="modalBatchId"></span></td>
                        <td><span id="modalTimeStamp"></span></td>
                        <td><span id="modalStopReason"></span></td>
                        <td><span id="modalMachineId"></span></td>
                    </tr>


                </table>
            </div>
            <div class="modal-footer">
                <button type="button" id="handleButton" onclick="handleAlarm()" class="handleAlarmButton">Handle alarm</button>
            </div>
        </div>

    </div>
    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - SCAMS</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
